# Cloud Run export job: BigQuery views -> cityperfume.json -> GCS.
# Pinned for reproducible container builds. This job only reads BigQuery views and writes
# JSON to GCS — it never touches the raw loaders (Google Ads DTS / Windsor / GA4 / Neto),
# so it needs nothing beyond the BigQuery + Storage clients.
google-cloud-bigquery==3.41.0
google-cloud-storage==3.10.1
