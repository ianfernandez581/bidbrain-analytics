Flask==3.0.3
gunicorn==23.0.0
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.20.2
google-cloud-run==0.10.11
google-auth==2.35.0
requests==2.32.3
PyJWT[crypto]==2.9.0
# Knowledge base (kb_index.py): the chunk vectors are one float32 matrix and cosine over the
# whole library is a single matrix product. Nothing else here needs it.
numpy==2.4.6
# PDF upload (kb_extract.py). Imported lazily, so an image without it still boots.
pypdf==6.14.2
# Phoenix span export (kb_trace.py), OFF unless PHOENIX_COLLECTOR_ENDPOINT is set and imported only
# when it is. The ENCODER half only: opentelemetry-exporter-otlp-proto-http would drag in a second
# HTTP stack, and this image already has `requests` to post the protobuf with.
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-common==1.44.0
# Word / PowerPoint / Excel readers for kb_extract (2026-09-15); imported lazily like pypdf
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
# kb_slack.py buckets messages into calendar days in Australia/Sydney via zoneinfo, which reads the
# system tz database. A slim image may not ship one; this is the pure-Python fallback, so a
# missing database degrades to UTC days silently instead of loudly - and this makes it not missing.
tzdata==2026.4   # as of 2026-09-17 (pip index versions tzdata)
