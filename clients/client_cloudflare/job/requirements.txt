# Cloud Run export job: BigQuery views -> cloudflare.json -> GCS.
# Pinned for reproducible container builds. This job no longer touches Snowflake
# (the shared ingest/snowflake_data_pull unit mirrors the source tables into
# raw_snowflake; the Cloudflare-specific static tables are seeded from data/ via
# seed_static.py; this job only reads BigQuery views and writes the JSON to GCS),
# so it needs nothing beyond the BigQuery + Storage clients. Matches client_mongodb/job.
google-cloud-bigquery==3.41.0
google-cloud-storage==3.10.1
