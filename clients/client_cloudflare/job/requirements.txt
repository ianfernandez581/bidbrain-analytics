# Cloud Run export job: Snowflake -> BigQuery -> cloudflare.json -> GCS.
# Pinned for reproducible container builds, resolved together against
# python:3.12-slim (pandas held <3 for ecosystem compatibility). Matches the
# pins in client_mongodb/job/requirements.txt so both client jobs share an image base.
snowflake-connector-python[pandas]==4.6.0
pandas==2.3.3
pyarrow==24.0.0
google-cloud-bigquery==3.41.0
google-cloud-storage==3.10.1
cryptography==48.0.0
# Only used by the LOCAL key fallback in main.py (_snowflake_key_bytes): when
# SNOWFLAKE_KEY isn't in the env, the key is read from Secret Manager via ADC.
# Cloud Run injects SNOWFLAKE_KEY directly so it never hits this path.
google-cloud-secret-manager==2.28.0
