# Cloud Run dash service: password gate + serves geocon.json + dashboard.html.
# Pinned for reproducible container builds. No AI report endpoint (geocon v1).
Flask==3.0.3
gunicorn==23.0.0
google-cloud-storage==2.18.2